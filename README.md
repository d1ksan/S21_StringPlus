## Implementation string.h

**Командный проект по реализации стандартной библиотеки string.h на языке C**

### 👥 Команда и распределение задач
- **bulakath** – функции `mem*` (`memchr`, `memcmp`), `strncpy`, `strlen`  
- **josefbar** – `sprintf` и `sscanf`  
- **tyberoru** – поисковые функции, `strtok`  
- **karrenkr** – специальные функции, `Makefile` + тесты  

### 🎯 Цели проекта
- Полная реализация функций библиотеки `string.h`
- Частичная реализация `sprintf`
- Unit-тестирование с покрытием >80%
- Сборка статической библиотеки `.a`

---

### ✅ Выполненная часть (bulakath)

Реализованы следующие функции стандартной библиотеки `string.h` (с префиксом `s21_`):

### 📦 Реализованные функции

| Функция          | Описание                                                                          | Прототип                                                      |
|------------------|-----------------------------------------------------------------------------------|---------------------------------------------------------------|
| `s21_strlen`     | Вычисляет длину строки (без учёта завершающего нуль-символа)                      | `s21_size_t s21_strlen(const char *str)`                     |
| `s21_strncpy`    | Копирует не более `n` символов из строки `src` в `dest`                           | `char *s21_strncpy(char *dest, const char *src, s21_size_t n)` |
| `s21_memset`     | Заполняет первые `n` байт памяти значением `c`                                    | `void *s21_memset(void *str, int c, s21_size_t n)`           |
| `s21_memcpy`     | Копирует `n` байт из `src` в `dest` (без учёта перекрытия)                        | `void *s21_memcpy(void *dest, const void *src, s21_size_t n)` |
| `s21_memchr`     | Ищет первое вхождение символа `c` в первых `n` байтах памяти                      | `void *s21_memchr(const void *str, int c, s21_size_t n)`     |
| `s21_memcmp`     | Сравнивает первые `n` байт двух областей памяти                                   | `int s21_memcmp(const void *str1, const void *str2, s21_size_t n)` |

Все функции полностью соответствуют поведению оригинальных функций из `string.h` (за исключением префикса `s21_`).

---

### 💻 Тестирование

Модульные тесты написаны с использованием фреймворка **Check**.  
Для каждой функции проверяется корректность работы на широком спектре входных данных, включая краевые случаи.

#### 📊 Количество тестов

| Функция          | Кол-во тестов | Особенности проверки                                      |
|------------------|---------------|-----------------------------------------------------------|
| `s21_strlen`     | 13            | строки с нуль-символом в середине, пустые строки         |
| `s21_strncpy`    | 9             | копирование с ограничением, дополнение нулями            |
| `s21_memset`     | 9             | заполнение произвольным символом, нулевая длина          |
| `s21_memcpy`     | 16            | перекрытие областей, копирование 0 байт                  |
| `s21_memchr`     | 11            | поиск в строке, массиве чисел, с плавающей точкой        |
| `s21_memcmp`     | 13            | сравнение регистров, чисел, длинных строк                |
| **ИТОГО**        | **71**        |                                                           |
---

### 🛠 Сборка и запуск

#### Статическая библиотека

Сборка статической библиотеки `s21_string.a` выполняется через `Makefile`. Для подключения достаточно слинковать библиотеку и включить заголовочный файл:
```c
#include "s21_string.h"
```
#### Ручная компиляция тестов

Для быстрой проверки можно собрать тестовый исполняемый файл напрямую:

```bash
gcc -Wall -Wextra -Werror s21_string.c s21_string_test2.c -o s21_string_test \
    -lcheck -lm -lpthread -lrt -lsubunit   
